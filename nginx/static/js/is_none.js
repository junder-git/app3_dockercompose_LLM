// =============================================================================
// SIMPLIFIED is_none.js - Just the guest session starter
// =============================================================================

// Simple guest session starter for unauthenticated users
document.addEventListener('DOMContentLoaded', () => {
    // The startGuestSession function is now handled by SharedInterface
    console.log('🚀 Guest session management ready (handled by SharedInterface)');
});